[toolchain]
channel = "1.81"
profile = "minimal"
# The fmt + clippy gates (CI and local) need these components on top of the
# minimal profile; declaring them here makes a fresh `rustup show` self-sufficient.
components = ["rustfmt", "clippy"]
