# The implementer's guide (placeholder)

The full guide lands with its own slice (runner contract, dispatch table, key census, corpus
schemas, permissiveness-mutation expectation). This placeholder keeps the curated reading
order wired; see the specification and the conformance corpus in the interim.
