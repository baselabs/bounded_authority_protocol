{"marks":[{"member":"nonce","reason":"optional proof claim: the corpus exercises nonce only through invalid_nonce rejection cells (check_envelope.invalid_nonce=1); no valid case carries a nonce, so the claim's valid-shape leg is unobserved by design of the current case set","set":"proof-claims"},{"member":"one_of","reason":"the corpus's valid selector cases cover kinds all and equals only (check_envelope.valid cells); one_of's valid-shape leg is unobserved by design of the current case set — its rejection legs are covered elsewhere","set":"selector-kinds"}]}
