defmodule BoundedAuthorityProtocol do
  @moduledoc """
  Deterministic protocol verification for cryptographically bounded
  proof-of-possession authority.

  Protocol behavior is exposed only through the explicit
  `BoundedAuthorityProtocol.V1` namespace, including named public submodules such as
  `BoundedAuthorityProtocol.V1.Json` and `BoundedAuthorityProtocol.V1.Base64Url`. There is no
  implicit latest profile and no duplicate unversioned façade.
  """
end
